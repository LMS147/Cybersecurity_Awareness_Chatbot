<Page x:Class="form.ActivityPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:form"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ActivityPage">

    <Grid>

        <!-- Activity Log Display -->
        <Border Grid.Column="1" Background="#252526" Padding="20" Margin="20">
            <StackPanel>
                <TextBlock Text="Activity Log" FontSize="20" Foreground="White" FontWeight="Bold" Margin="0,0,0,20"/>
                <ListView x:Name="ActivityLogList" Background="#2D2D30" Foreground="White" FontSize="14">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Date" Width="100" DisplayMemberBinding="{Binding Date}"/>
                            <GridViewColumn Header="Time" Width="100" DisplayMemberBinding="{Binding Time}"/>
                            <GridViewColumn Header="Activity Type" Width="150" DisplayMemberBinding="{Binding ActivityType}"/>
                            <GridViewColumn Header="Message" Width="350" DisplayMemberBinding="{Binding Message}"/>
                        </GridView>
                    </ListView.View>
                </ListView>
            </StackPanel>
        </Border>

    </Grid>
</Page>
